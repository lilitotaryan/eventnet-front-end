import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    maxWidth: 500,
  },

  text: {
    margin: '15px',
  },

  title: {
    flex: 1,
  },
}));

export { useStyles };
